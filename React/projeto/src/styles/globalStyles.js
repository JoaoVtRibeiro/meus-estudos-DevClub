/* 
    Importação do recurso "createGlobalStyle" da biblioteca "styled-components"

    Esse recurso permite criar estilos globais para todos os elementos
*/
import { createGlobalStyle } from "styled-components";

export default createGlobalStyle` // Exportando os estilos

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Roboto', sans-serif;
    font-weight: 700;
    text-decoration:none;
}`;


