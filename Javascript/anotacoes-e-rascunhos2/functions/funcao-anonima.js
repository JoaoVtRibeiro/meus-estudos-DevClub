/* 
    Função Anônima

    É bem parecido com a função comum, a unica coisa que muda é que a 
    função é utilizada como valor de uma variavel, não precisando de um nome.

*/

let sum = function(number1, number2){
    return number1 + number2
}

let result = sum(5,4)

console.log(result)